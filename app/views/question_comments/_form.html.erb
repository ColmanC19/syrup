<%= form_with(model: question_comment, local: true) do |form| %>
  <% if question_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_comment.errors.count, "error") %> prohibited this question_comment from being saved:</h2>

      <ul>
      <% question_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :body_text %>
    <%= form.text_area :body_text %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
